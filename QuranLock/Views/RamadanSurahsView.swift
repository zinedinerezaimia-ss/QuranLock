import SwiftUI

struct RamadanSurahsView: View {
    var body: some View {
        ZStack {
            Theme.primaryBg.ignoresSafeArea()
            Text("Sourates Ramadan")
                .foregroundColor(.white)
                .font(.title)
        }
    }
}
