import SwiftUI

struct RamadanDuaasView: View {
    var body: some View {
        ZStack {
            Theme.primaryBg.ignoresSafeArea()
            Text("Duaas Ramadan")
                .foregroundColor(.white)
                .font(.title)
        }
    }
}
